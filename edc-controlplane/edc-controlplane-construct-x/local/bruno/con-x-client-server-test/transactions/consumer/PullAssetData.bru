meta {
  name: PullAssetData
  type: http
  seq: 6
}

get {
  url: {{EDR_PUBLIC_ENDPOINT}}
  body: none
  auth: inherit
}

script:pre-request {
  req.setHeader("Authorization", bru.getEnvVar("pullSecret"));
}

settings {
  encodeUrl: true
  timeout: 0
}
