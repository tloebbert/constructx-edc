meta {
  name: PullAssetData
  type: http
  seq: 6
}

post {
  url: {{PROVIDER_DATAPLANE_PUBLIC}}
  body: json
  auth: inherit
}

body:json {
  {
    "addresses": [
      { 
        "country-code": "de",
        "city": "Berlin"
      }
    ]
  }
}

script:pre-request {
  req.setHeader("Authorization", bru.getEnvVar("pullSecret"));
}

settings {
  encodeUrl: true
  timeout: 0
}
